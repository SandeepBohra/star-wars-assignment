import React, { Component } from 'react';
import connect from 'react-redux';
import rootReducer from '../reducers/rootReducer';

class App extends Component {
    render() {
        return (
            <div>
                
            </div>
        );
    }
}

/* const mapStateToProps = (state) =>{
    return {
        starWarsData : allReducers.state
    }
} */

export default App;